{
  "name": "ardrone-operator",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "postinstall": "typings install && typings install express-serve-static-core --ambient && typings install serve-static --ambient",
    "start": "tsc && concurrently \"tsc -w\" \"nodemon build/server/server.js\"",
    "clean": "rm -rf build && rm -rf typings"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "ar-drone": "0.3.3",
    "express": "^4.13.4",
    "nodejs-websocket": "^1.6.0"
  },
  "devDependencies": {
    "browser-sync": "^2.12.8",
    "concurrently": "^2.0.0",
    "nodemon": "^1.9.2"
  }
}
